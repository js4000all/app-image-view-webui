# tool-image-view-webui
ローカルファイルを閲覧するシンプルなwebアプリ。

## 起動方法
```sh
python app.py /path/to/image-dir
```

## 構成と機能

- 折りたたみできるサイドメニュー
  - 起動時に指定したディレクトリのサブディレクトリを選択するプルダウン
  - サブディレクトリ内の画像ファイルリスト
- サブディレクトリ内の画像をメイン領域に拡大表示
- 左右キーで画像切り替え
