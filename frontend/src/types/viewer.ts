export type ViewerDirectoryEntry = {
  directory_id: string
  name: string
}

export type ViewerImageEntry = {
  file_id: string
  name: string
}

export type ViewerImageMetadata = {
  file_id: string
  name: string
  directory_id: string
  directory_name: string
}
