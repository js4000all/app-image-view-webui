{
  "name": "app-image-view-webui-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:home": "vite build --config vite.home.config.ts && rm -rf ../static/home-app && mkdir -p ../static/home-app && cp -R dist-home/* ../static/home-app/",
    "build:viewer": "vite build --config vite.viewer.config.ts",
    "build:bundle": "npm run build:home && npm run build:viewer",
    "check": "npm run build:bundle"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
